claude-agent-sdk
aiohttp>=3.9
pyyaml>=6.0
InquirerPy>=0.3.4

# Slack (install if using Slack channel)
# slack-bolt>=1.18
# slack-sdk>=3.26

# Telegram (install if using Telegram channel)
# aiohttp is already required above

# Remote deployment (install if using /setup-remote-project or /setup-remote-workspace)
requests>=2.31
